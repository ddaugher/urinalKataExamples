import spock.lang.Specification

public class UrinalKataSpec extends Specification {

  def "first failing test in order to get the project up and running"() {
    when: "instantiate object"

    then: "expect proper values"
    true == false
  }

}
